package my.curator;

import org.apache.zookeeper.CreateMode;

public class UtilCurator {
//获得CuratorFramework
//创节点 节点类型分为临时节点(CreateMode.EPHEMERAL) ,临时顺序节点 (CreateMode.EPHEMERAL_SEQUENTIAL),永久节点 (CreateMode.PERSISTENT),永久顺序节点(CreateMode.PERSISTENT_SEQUENTIAL)
//删除
//修改
//查询
//将查询字符串转换
//添加acl
//添加监听事件

}